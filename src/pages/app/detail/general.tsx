import React from 'react';
import GeneralForm from 'src/sections/apps/Detail/General/GeneralForm';

const General = () => {
  const a = 12;
  return <GeneralForm />;
};

export default React.memo(General);
