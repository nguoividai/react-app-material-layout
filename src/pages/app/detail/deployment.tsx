import React from 'react';
import DeploymentForm from 'src/sections/apps/Detail/Deployment/DeploymentForm';

const Deployment = () => {
  const a = 12;
  return <DeploymentForm />;
};

export default React.memo(Deployment);
