import React from 'react';
import ConfigurationForm from 'src/sections/apps/Detail/Configuration/ConfigurationForm';

const Configuration = () => <ConfigurationForm />;

export default React.memo(Configuration);
